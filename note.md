Flow thực tế cho bạn
Google Sheet (mọi người điền) 
    ↓ publish as CSV
Web app đọc CSV 
    ↓ parse data
Piped.video/embed/VIDEO_ID  ← không ads, không cần host gì
    ↓ deploy lên
Vercel / Netlify / GitHub Pages (free, 2 phút)
    ↓
Ném link cho cả công ty dùng
Hoàn toàn free, không cần server, không cần maintain gì cả!

Chỉ cần làm 3 bước
Bước 1 — Publish Google Sheet thành CSV public

File → Share → Publish to web → CSV → lấy link

Bước 2 — Deploy HTML file lên Vercel/GitHub Pages

Drag & drop file là xong

Bước 3 — Ném link cho team dùng 🎉